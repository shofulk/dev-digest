# `@devdigest/web` — agent instructions

The canonical file is `AGENTS.md`, next to this one. This `CLAUDE.md` exists only for
Claude Code older than 2.1.277, which does not read `AGENTS.md` natively; it imports the
canonical file whole. Edit `AGENTS.md` — nothing belongs here.

@AGENTS.md
