/** Confidence bar colour thresholds — mirrors the ConfidenceNum bands. */
export const CONFIDENCE_OK = 0.85;
export const CONFIDENCE_WARN = 0.65;

/** Width of the confidence bar (px) and of the action column (px). */
export const CONFIDENCE_BAR_WIDTH = 140;
export const ACTIONS_WIDTH = 176;
