/** Rationale is a paragraph; the popover shows only its opening. */
export const RATIONALE_PREVIEW_CHARS = 150;

/** Popover box size, also used to keep it inside the viewport. */
export const POPOVER_WIDTH = 420;
export const POPOVER_MAX_HEIGHT = 380;

/** Gap between the anchoring cell and the popover. */
export const ANCHOR_OFFSET = 6;
