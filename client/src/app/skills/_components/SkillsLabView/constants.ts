/** Route of the Skills Lab. */
export const SKILLS_PATH = "/skills";
/** Query params that carry the view state. */
export const SKILL_PARAM = "skill";
export const TAB_PARAM = "tab";
/** Left column width (px). */
export const LIST_WIDTH = 340;
/** AppShell topbar height (px), subtracted from the viewport for the two panes. */
export const TOPBAR_HEIGHT = 52;
