/** Width (px) of the diff dialog — wide enough for a typical markdown line. */
export const DIFF_DIALOG_WIDTH = 860;

/** Width (px) of the restore confirmation. */
export const RESTORE_DIALOG_WIDTH = 460;

/** Symbols that mark a diff line by more than colour. */
export const DIFF_SYMBOL = { add: "+", del: "−", same: " " } as const;
