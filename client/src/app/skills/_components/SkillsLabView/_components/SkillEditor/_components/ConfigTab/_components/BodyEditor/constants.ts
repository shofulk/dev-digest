/** Quiet period (ms) after the last keystroke before the exact token count is requested. */
export const TOKEN_DEBOUNCE_MS = 400;

/** Editor geometry (px). Gutter and textarea share `LINE_HEIGHT` and `PAD_Y` so line n stays beside number n. */
export const LINE_HEIGHT = 20;
export const PAD_Y = 12;
export const EDITOR_HEIGHT = 380;
/** Extra gutter padding so it can scroll as far as a textarea that shows a horizontal scrollbar. */
export const SCROLLBAR_ALLOWANCE = 16;
