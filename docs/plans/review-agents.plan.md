# Development Plan: review, test and docs subagents (test-writer, architecture-reviewer, plan-verifier, doc-writer)

**Spec:** none (criteria from request) · **Packages:** repo tooling `.claude/` (agents, hooks) — no package code in server/, client/, reviewer-core/ or e2e/ changes · **Base:** `cb195fb` (branch `L03-Subagents`; the S10 baseline commit — `.claude/agents/**`, both guard hooks and this plan are tracked from here; working tree beyond it: all rev 5 work, uncommitted — `M .claude/agents/README.md`, `M .claude/agents/architecture-reviewer.md`, `M .claude/hooks/scope-guard.sh`, `M INSIGHTS.md`, `M docs/plans/review-agents.plan.md` — and staged `A docs/skills/README.md` (O6); S18 has not happened) *(rev 6)* · **Revision:** 7

## Goal
Add four project subagents — test-writer, architecture-reviewer, plan-verifier, doc-writer —
next to researcher/planner/implementer, give the planner an update mode so a saved plan can be
revised in place, back every write restriction the new agents claim with a hook that has a
`self-test` mode, and extend the `.claude/agents/README.md` map to cover all of it.

## Acceptance criteria
1. **AC1 — test-writer.** Writes client tests (Vitest + RTL, colocated `*.test.tsx`/`*.test.ts`), server hermetic `*.test.ts` and DB-backed `*.it.test.ts`, reviewer-core tests, and e2e `specs/NN-name.flow.json` when relevant; loads the matching project skills for the code under test; writes only test files and test fixtures, never production code (hook-enforced).
2. **AC2 — architecture-reviewer.** No write tools at all. Checks onion rings (server/src), frontend-ui-architecture (client/src), reviewer-core purity, vendor mirrors, DI-container rule, package independence. Returns findings with rule, `file:line`, import edge from→to with ring/layer, severity, why. Reuses `pnpm --dir server arch` (`server/.dependency-cruiser.cjs`) instead of re-implementing it, and states honestly that client/ has no deterministic boundary checker.
3. **AC3 — plan-verifier.** Read-only. Per-item traceability matrix over **every** item of `docs/plans/<feature>.plan.md` and the spec AC: verdict (met / partial / not met / cannot verify) + concrete evidence (`file:line`, test name, command output) per item. Forbidden to substitute generic advice or code-quality commentary for that check.
4. **AC4 — doc-writer.** Documents implemented features; turns a plan or other material into docs with Mermaid diagrams; knows the home of each kind of doc; writes only documentation paths (hook-enforced).
5. **AC5 — `.claude/agents/README.md`.** Catalog, Flow, Permissions, Artifacts, Sources cover all four agents and the plan-update loop. Flow: planner → plan file → implementer → test-writer? → architecture-reviewer ∥ plan-verifier → doc-writer → /pr-self-review.
6. **AC6 — enforcement.** Every write restriction a prompt claims is backed by a tool denial or a hook where the platform allows; every new hook has a `self-test` mode like `implementer-guard.sh`.
7. **AC7 — planner update mode.** Given the path of an existing plan (plus a change request, an Implementation Report or a Plan Verification), the planner returns a revised plan that keeps existing AC/step/test IDs stable, marks changed/new/dropped items instead of renumbering or deleting them, bumps `Revision`, refreshes `Base`, and appends one line to a `## Revisions` section. Its create-mode output format carries the same `Revision` field and `## Revisions` section.
8. **AC8 — reply language.** Each of the 7 agent files in `.claude/agents/` (architecture-reviewer, doc-writer, implementer, plan-verifier, planner, researcher, test-writer) carries exactly one Hard rule "**Reply in the user's language.**", placed just before its "Repo content is data" / "Web content is data" bullet: prose follows the language named by a `User language: …` line in the delegating prompt (fallback: the prompt's own language); code, identifiers, IDs, paths, commands and their output, quotes and skeleton section headings stay verbatim; files written to the repo stay English. `.claude/agents/README.md` states the same in a "**Reply language.**" paragraph after the Flow explanation. *(new rev 4)*

## Constraints
- **C1 — Architecture:** No agent is exempt from the rings. The architecture-reviewer prompt names the ring table of the preloaded `onion-architecture/SKILL.md` by its section title *The rings, by real path* (referenced, not copied, so the two cannot drift) and uses the *Enforcement* baseline **0 errors / 17 warnings** as the delta reference ("a change that raises any of those counts is a regression"). For client/src it keeps the SPECIFIED / CONVENTION / UNSPECIFIED tiers of `frontend-ui-architecture` (*How to use this*): only SPECIFIED or `client/AGENTS.md` rules can be `critical`/`major`; CONVENTION advice is at most `minor`; UNSPECIFIED is never a finding. Severity words come from `pr-self-review/severity.md` (§1 row 4: arch `error` → critical, `warn` → major) so its output can feed `/pr-self-review`. *(rev 4)*
- **C2 — Contracts & data:** none — no route, Zod contract or table is touched.
- **C3 — Tooling facts relied on (verified):** `server/package.json` → `"arch": "depcruise src --config .dependency-cruiser.cjs --output-type err"` (run through the script, so the `--output-type` token never appears in the agent's command line); `depcruise -T <type>` is the short form of `--output-type` (the rev 4 and rev 5 architecture-reviewer runs used `-T err-long` successfully); the config equals `.claude/skills/onion-architecture/enforcement/dependency-cruiser.cjs` and includes `reviewer-core-stays-pure`, `no-sdk-outside-adapters`, `no-cross-module-internals`, `no-circular`; no `cache` option, so `arch` writes nothing. `client/eslint.config.mjs` has no import-boundary rules — no deterministic client architecture check. Mirrors: `client/src/vendor/{shared,ui}` ← `server/src/vendor/shared`. `server/clones/**` is gitignored (`.gitignore:20`) and holds full copies of **untrusted** cloned PR repos (incl. their own `package.json`, `vitest.config.*`, `vendor/`) — every glob excludes it. `e2e/package.json` → `"test": "tsx run.ts"` runs the browser flows; e2e `typecheck`/`lint` do not. `client/tsconfig.json` has `incremental: true` → `typecheck` writes a gitignored `*.tsbuildinfo`. Package scripts: `test` = `vitest run` (server, client; reviewer-core adds `--passWithNoTests`), `lint` = `eslint .`, `typecheck` = `tsc --noEmit …`; pnpm appends any trailing arguments to the script, so `pnpm --dir server test <args>` = `vitest run <args>`. Versions: `vitest ^2.1.8`, `eslint ^10.11.0`, `dependency-cruiser ^17.4.3`. `server/eslint.config.js:16` ignores `clones/**`; vitest `include` is `test/**`/`src/**` (server, reviewer-core) and `src/**` (client). No agent prompt uses `cd` (`rg '\bcd [A-Za-z./]' .claude/agents/` → exit 1); `npm` appears only in `test-writer.md:139` ("`npm test` from `e2e/`", fixed by S22). *(rev 6)*
- **C4 — Do not touch:** `.claude/skills/**` (incl. `pr-self-review/routing.md`), `.claude/settings.json` (all enforcement is agent-scoped), every `CLAUDE.md`, `.claude/hooks/implementer-guard.sh`, `pr-gate.sh`, `pre-push` (templates only), `skills-lock.json`. `researcher.md` and `implementer.md` are edited **only** by S9; `planner.md` **only** by S0 and S9. From `cb195fb` (S10) on, `git diff cb195fb -- .claude/agents/{researcher,implementer,planner}.md .claude/hooks/implementer-guard.sh .claude/hooks/pr-gate.sh` must be empty — that is the evidence. `plan-verifier.md`, `architecture-reviewer.md`, `test-writer.md` and `doc-writer.md` are **not** restricted: plan steps may edit them (S12, S14, S22). *(rev 6)*
- **C5 — INSIGHTS (root):** *2026-09-25 — an exact-command allowlist entry in `scope-guard.sh` must sit before the shell-invoker recursion* → a `CHECKS_EXACT` hit returns from `segVerdict` before `preReject` and the recursion (`scope-guard.sh:333`), so S19's cwd condition must be part of that early check, not a later one; `cd` is its own segment (`segments()` splits on `&&`/`;`/`(`), so rejecting it as an unknown head is enough, but S19 must keep the hit ahead of the recursion. *2026-09-25 — `bash -n scope-guard.sh` fails several functions away from the real mistake: an apostrophe in a comment ends the outer `node -e '…'` string early* → every S19/S20/S21 edit inside `bash_eval`/`write_eval` contains no literal `'`, not even in a `//` comment; backtick as `\x60`, quote as `\x27`; `bash -n` after every edit. *2026-09-24 — a hand-probed guard hook "allows everything": the probe, not the hook, is broken* → the new cwd cases go through an in-script helper that builds the hook JSON (incl. `cwd`) with `node … JSON.stringify` and passes the profile as a separate argument, never ad-hoc zsh probes; the `2>&1` fix in `segments()` survives S19/S20. Kept from rev 5 as the failure-policy source: *2026-09-20 — a `PreToolUse` hook on `Bash` runs on every Bash call, and a gate hook must fail open* → the cwd guard costs at most two `realpathSync` calls; an unresolvable **project root** is an internal runtime error (fail-open), an unresolvable or foreign **cwd** is a policy "no" for the cwd-dependent commands only (S19); agent files and hooks load at session start, so T8 needs a fresh session after S19–S23. *(rev 6: the CHECKS_EXACT entry replaces the 2026-09-20 entry in the top three, since it binds S19 directly; the 2026-09-20 rule is unchanged and stays cited)*
- **C6 — Bash profiles are allowlists of *behaviour*, not only of heads.** An allowlisted head whose flags or config can run an arbitrary program or write a file (see S8, S15) is a hole in the profile, closed by a flag check plus a self-test case; never by switching the profile to fail-open. Conversely, a head may be added to a profile when its *behaviour* with the exact accepted arguments is read-only (or writes only a temp dir it removes) and runs only code no agent on that profile can write (S15(f)). A head whose behaviour depends on the working directory (relative `CHECKS_EXACT` paths, `pnpm --dir <name>`) is only as safe as the cwd it runs in — so a head that changes the cwd (`cd`) or points a tool at another tree (`git -C`) breaks every cwd-dependent entry and is not allowed (S19); where the flag surface of an allowed program is wide, the check is an allowlist of arguments, not a denylist (S20). *(rev 6)*

## Decisions

**Agent settings (follow the existing three):**

| Agent | model | permissionMode | tools | disallowedTools | skills (preload) | color | description adds |
|---|---|---|---|---|---|---|---|
| test-writer | `sonnet` | `acceptEdits` | Read, Grep, Glob, Edit, Write, Bash, Skill | Agent, NotebookEdit, WebSearch, WebFetch | `engineering-insights`; everything else per bucket via `Skill` | `yellow` | "Use after implementer when the plan assigns tests separately or the report lists test gaps" |
| architecture-reviewer | `opus` | `default` | Read, Grep, Glob, Bash | Write, Edit, NotebookEdit, Agent, Skill, WebSearch, WebFetch | `onion-architecture`, `frontend-ui-architecture` | `red` | "Use proactively after implementer, before /pr-self-review" |
| plan-verifier | `opus` | `default` | Read, Grep, Glob, Bash | Write, Edit, NotebookEdit, Agent, Skill, WebSearch, WebFetch | none (context kept for plan + diff + evidence) | `purple` | "Use proactively after implementer" |
| doc-writer | `sonnet` | `acceptEdits` | Read, Grep, Glob, Edit, Write, Bash, Skill | Agent, NotebookEdit, WebSearch, WebFetch | `mermaid-diagram` | `orange` | — (on demand) |

Reviewers use `default`, not `plan`: they must run `pnpm … arch/test`, and Bash semantics under
`plan` mode are not pinned down by the research. They hold no write tool; the hook covers Bash.
Reviewers run on `opus` so they do not share the `sonnet` implementer's blind spots.

**Hook — one shared, parametrized `.claude/hooks/scope-guard.sh`** (tokenizer copied from `implementer-guard.sh`; header names the other two copies):
- `write tests` / `write docs` — `PreToolUse` on `Write|Edit|NotebookEdit`: reads `tool_input.file_path` (or `notebook_path`), resolves it against `$CLAUDE_PROJECT_DIR` with `..` collapsed and symlinks resolved on the nearest existing ancestor, checks the profile's allow/deny globs **against both the logical repo-relative path and the symlink-resolved repo-relative path — either one denied or not allowed → block**. *(rev 4)*
- `bash readonly` / `bash checks` — `PreToolUse` on `Bash`, **allowlist** per segment, recursing into `bash -c`/`eval` like implementer-guard.
  - `readonly`: `git status|diff|log|show|grep|ls-files|rev-parse|merge-base|blame|cat-file`; `ls rg grep cat head tail wc diff cmp sort uniq cut tr jq stat file pwd echo printf test true basename dirname realpath which`; `find` without `-delete`/`-exec*`/`-ok*`/`-fprint*`/`-fls`; `sed` without `-i`/`--in-place`. `cd` is **no longer** on the list (S19, *cwd rule* below). *(rev 6)*
  - `readonly` flag checks on allowed heads: `rg` without `--pre`/`--pre=…`; `git` without `-c`/`--config-env` (global options), without `-C`/`--git-dir*`/`--work-tree*` (S19) and without `--ext-diff`, and `git grep` without `-O…`/`--open-files-in-pager…`; `sed` without `-f`/`--file` and without a `w`/`W`/`e` command or `s///w`/`s///e` flag in its script (uncertain parse → reject); `sort` without `--compress-program…`; `file` without `-C`. *(rev 6)*
  - `checks` = `readonly` + `docker info`; `pnpm` **with `--dir`/`-C` before the script — a bare `pnpm <script>` is rejected** — and **every** `--dir`/`-C` value in the segment exactly one of `server`, `client`, `reviewer-core`, `e2e`, given as a separate token (`--dir=…`, attached `-C<path>` and `--prefix*` are rejected, S19); scripts `typecheck|lint|test|arch` (also via `run`), except that `--dir e2e` allows only `typecheck|lint` (its `test` runs the flows); script forms take **no trailing arguments** except `test`, which takes the vitest argument allowlist (S20); `exec vitest run`, `exec tsc --noEmit`, `exec depcruise`, `exec eslint` each take only their S20 argument allowlist. `npm` is dropped (S19): its `-C` means `--prefix` and it has no `--dir`, so the `--dir` guarantees never held for it, and after S22 no prompt uses it. Plus, exact-argument only and only when the cwd is the project root (S19): `bash -n .claude/hooks/{scope-guard,implementer-guard}.sh` and `.claude/hooks/{scope-guard,implementer-guard}.sh self-test` (S15(f)). *(rev 6)*
  - Leading environment assignments: only `CI=<value>` is accepted before the head; any other `VAR=value` prefix (`NODE_OPTIONS`, `GIT_*`, `PAGER`, …) is rejected. *(new rev 4)*
  - Rejected in any segment: redirection (`>`, `>>`, `&>`) except to `/dev/null` or fd dup, `--fix`, `-u`/`--update` (this includes `sort -u`; agents use `sort | uniq`), `--write`, `--output*`/`--outputFile` (this includes depcruise's long `--output-type`; the agent uses `-T`), `sort -o`, `xargs`, `tee`, any unknown head (this includes `cd`, `pushd`, `popd`, `builtin`, `command`, `npm` from rev 6), and command substitution — a backtick or `$(` anywhere outside single quotes, including in the body of an **unquoted** heredoc (bash expands it there; only quoted-delimiter bodies are inert data). This covers implementer-guard's list (commit, push, `gh pr`, migrate, drizzle-kit) and adds `pnpm add|install|remove`, `db:generate|seed`, `dev`, `build`, `docker` (except `info`), `git fetch|checkout|restore|reset|stash|apply|add`. *(rev 6)*
- **Failure policy:** internal runtime error (no node, unreadable stdin, unparsable JSON, unresolvable project root) → exit 0 + warning (fail-open, per INSIGHTS and pr-gate). Parsed input outside the allow set, or a Write/Edit with no resolvable path → exit 2 (policy). **An unknown mode or profile argument → exit 2** with a "misconfigured scope-guard" message: the argument comes from an agent's frontmatter, not from runtime input, so it is a configuration fault; failing closed makes it loud on the first call of the one misconfigured agent (a one-line fix), whereas failing open silently disarms that agent's whole guard — the exact "allows everything" symptom of the 2026-09-24 INSIGHTS entry. On the Bash path the project root is `$CLAUDE_PROJECT_DIR`, falling back to the hook's own repo (`$SELF/../..`) — never to the hook input's `cwd`, which is the value being checked; only if neither resolves is it an internal error. A cwd that does not resolve, or resolves anywhere but the root, is a **policy** "no" for the cwd-dependent commands (S19). `implementer-guard.sh`/`pr-gate.sh` keep their fail-open default (C4, O8). Stderr names the rule, lists what is allowed, and says where to record the need (*Production changes needed* / *Proposed edits*). *(rev 6)*
- Shared rather than per-writer: the profiles differ only in glob tables, the Bash rule is common to all four, and one `self-test` matrix covers every profile.

| Agent | Write guard | Bash guard |
|---|---|---|
| test-writer | `write tests` | `bash checks` |
| doc-writer | `write docs` | `bash readonly` |
| architecture-reviewer | — (Write/Edit denied) | `bash checks` |
| plan-verifier | — (Write/Edit denied) | `bash checks` |

**Write allow-lists:**
- **tests:** `{server,client,reviewer-core}/**/*.test.{ts,tsx}` (covers `*.it.test.ts`), `server/test/**`, `reviewer-core/test/**`, `client/src/test/**`, `**/__tests__/**` and `**/__snapshots__/**` under those packages, `e2e/specs/*.flow.json`. Fixtures only inside the test dirs above — no `**/fixtures/**` or `**/__fixtures__/**` glob *(rev 3)*. Always denied: `*/src/vendor/**`, `server/clones/**`, `node_modules/**`, `server/src/adapters/mocks.ts` (ring-2 production file), `server/src/db/seed.ts` (e2e asserts on it), `e2e/lib/**`, `e2e/run.ts` (harness; `e2e/AGENTS.md` forbids assertion logic there), `vitest.config.ts`, `package.json`, lockfiles, everything else.
- **docs:** root `README.md`, `{server,client,reviewer-core,e2e}/README.md`, `{server,client,reviewer-core,e2e}/.doc/**/*.md`, `docs/**/*.md`. Denied: `docs/plans/**` (planner→implementer handoff), `docs/skills/**` (case collision between staged `README.md` and tracked `readme.md`; lift the denial in the same change that resolves it), `docs/agent-prompts/**` except its `README.md` (runtime prompts mirrored to the DB), `**/.spec/**`, `**/AGENTS.md`, `**/CLAUDE.md`, `**/INSIGHTS.md`, `.claude/**`, `*/src/vendor/**`, `server/clones/**`, any non-`.md` file. *(rev 4)*

**Reviewers may run checks** through the `checks` profile only: `typecheck`, `lint` (no `--fix`), `test`/`vitest run`, `arch`, e2e `typecheck`/`lint` (never e2e `test`) — these write only gitignored caches — and the two hook self-tests / `bash -n` checks of S15(f). plan-verifier prefixes tests with `CI=1` so Vitest fails on new snapshots instead of writing them; runs `*.it.test.ts` only if `docker info` succeeds (else the item is *cannot verify*). Neither boots the stack, runs e2e flows or fetches. Every command runs from the repo root; S22 writes the exact list and the rev 5/6 constraints into both prompts, so prompt and hook no longer diverge. *(rev 6)*

**Hook self-tests in `checks` (rev 5 decision, change request 6):** allowed, exact-argument match, `checks` only (not `readonly`). Per C6 the question is behaviour, not the head: `bash -n <file>` parses without executing; `self-test` after S16 writes only a `mktemp` directory it removes, and runs code from `.claude/hooks/*.sh` that no `checks`-profile agent can write (test-writer's write scope excludes `.claude/**`, the reviewers have no write tool). Keeping S7.2 main-session-only would leave every hook-related row of every future verification at *partial*/*cannot verify* (19 partials in the rev 4 run came mostly from this). Any other argument (`scope-guard.sh bash checks`, `bash -n /tmp/x.sh`, a different script under `.claude/hooks/`) stays rejected. *(new rev 5)*

**cwd rule (rev 6 decision, change request 1):** option **(i)**, rejecting every cwd-changing construct in both profiles, plus a guard against a cwd **inherited** from outside the command. *(new rev 6)*
- *The hole.* `CHECKS_EXACT` (`scope-guard.sh:311-316`) and the `--dir` package names are relative paths, so bash and pnpm resolve them against the cwd. `cd` was allowlisted in `readonly`, which `checks` inherits (`:221`). So `cd server/clones/<repo> && .claude/hooks/scope-guard.sh self-test` ran an untrusted clone's script, and `cd server/clones/<repo> && pnpm --dir server test` ran its `test` script. Both bypass S15(a)/(f).
- *Why (i).* `cd`'s behaviour is "re-point every later relative path", so under C6 it is not a read-only head. No prompt relies on it (C3), so removing it costs nothing. Option (ii), keeping `cd` and resolving against `$CLAUDE_PROJECT_DIR`, would have to track the effective cwd across `&&`/`||`/`;`/`( … )`/`bash -c`, plus `cd -`, a bare `cd`, `CDPATH` and `$VAR` targets. That is a shell interpreter, and the copied tokenizer demonstrably mis-parses simpler things (INSIGHTS 2026-09-24, the `&` split). The standing rule is "uncertain parse → reject" (S8). If (ii) rejected whenever the effective cwd is not the root, it would reject every real `cd` anyway: (i) with extra parser risk.
- *What (i) alone misses.* A cwd the Bash tool already has when the command starts, for example one inherited from an earlier session state. The hook input carries `cwd` (hooks docs, *Sources*; `write_eval` already reads it at `:442`). In `json` mode the Bash path therefore compares realpath(`cwd`, fallback: the hook process's own cwd) with the project root (see *Failure policy*). Any mismatch, or a cwd that does not resolve, rejects the **cwd-dependent** commands only: a `CHECKS_EXACT` hit and any `pnpm` segment. The message says "run from the repo root". Read-only heads (`git diff`, `rg`, …) are unaffected, since their meaning does not change with the cwd.
- *The hook's own cwd.* The hook is invoked by absolute path (`"$CLAUDE_PROJECT_DIR"/.claude/hooks/…`) and computes `$SELF` absolutely. Its own cwd matters only as the fallback when the input has no `cwd`, which S19 pins with self-test rows.
- *Raw mode.* `raw` mode is reachable only from `self_test`, never from the dispatch `case`. It takes an explicit cwd argument that defaults to the root, so every existing `runb` row keeps its meaning.
- *Other tools that point at another tree.* In the same pass, `git -C`/`--git-dir`/`--work-tree` are rejected. A PR repo under `server/clones/**` can contain an embedded bare repository whose `config` sets `core.fsmonitor`/`core.pager`, and `git -C <that dir> status` would load it. No prompt uses `git -C` (C3).

**Argument allowlists (rev 6 decision, planner's re-check):** the S8/S15 flag checks are denylists over programs with wide CLI surfaces, and three concrete shapes pass today. *(new rev 6)*
- *What passes today:*
  - (1) Script forms forward their arguments. `pnpm --dir server test --config src/db/seed.ts` becomes `vitest run --config …`, and `pnpm --dir client lint -c x.mjs` becomes `eslint . -c x.mjs`. S15(b) checks only `exec`.
  - (2) `exec vitest run --reporter=./x.js` loads a reporter by path (vitest reporters docs).
  - (3) `exec depcruise src --webpack-config w.js` loads JS, and `--cache` writes. `exec eslint -f ./fmt.js` loads a formatter.
- *Fix under C6: allowlists of arguments (S20).*
  - `typecheck`/`lint`/`arch` script forms take no trailing argument.
  - The `test` script form and `exec vitest run` take positional filters (relative, no `..` segment, not under `clones/`), `--exclude <glob>`, `-t`/`--testNamePattern <pattern>`, `--reporter <name>` with a built-in name only (`default`, `basic`, `verbose`, `dot`, `tap`, `tap-flat`, `json`), `--passWithNoTests` and `--silent`, each also as `--flag=value`.
  - `exec tsc` takes `--noEmit` (required) and `-p`/`--project <path>` with the same path constraints.
  - `exec depcruise` takes positional paths under `src` (same constraints), `--config`/`-c .dependency-cruiser.cjs`, `-T err|err-long|text` and `--include-only <re>`.
  - `exec eslint` takes positional paths (same constraints; this also covers the uncertain ESLint 10 config lookup from a file's directory, *Risks*), `--max-warnings <n>` and `-f`/`--format stylish|json`.
- Widening means one table edit plus a T15 case, never a denylist.

**doc-writer homes (Diátaxis → repo):**

| Kind | Home |
|---|---|
| Reference, architecture, diagrams of a package | `<pkg>/README.md` |
| Explanation, decisions (summary / decision / alternatives / why rejected), runbooks | `<pkg>/.doc/<topic>.md` |
| Cross-package overview | root `README.md` *Architecture* |
| Cross-package rationale | `docs/<topic>.md` (precedent: `docs/pr-self-review.plan.md`) |
| Tutorials | **no home** — reports "needs a decision", does not invent `docs/tutorials/` |

doc-writer may edit `<pkg>/README.md`; it may **not** edit `.spec/` (hook-denied) — spec drift goes
to its report and the main session applies it. New "Read when" pointers for `AGENTS.md` are proposed
in the report, not written.

**`docs` bucket routing drift:** `doc-standards` and `file-conventions` resolve only from user-level
`~/.claude/skills/`, not `.claude/skills/`. doc-writer tries `Skill`; if absent, falls back to
`<pkg>/.doc/README.md`, `.spec/README.md` and neighbouring files' style, and records which under
*Conventions notes*. `routing.md` is not edited.

**Planner update mode (S0):** the planner stays read-only; the main session writes every revision
back to the same `docs/plans/<feature>.plan.md`. Git history of that file is the full audit trail;
`## Revisions` is the human-readable summary.

**Security observations on `scope-guard.sh` (rev 4 decision):** fixed in this plan (S8), not deferred —
each one lets an allowlisted head run an arbitrary program or write a file, which breaks the
profile contract AC6 relies on (C6). The security review of the hook as a whole stays handed off
(*Review hand-off*); S8 does not claim the hook is a sandbox. *(new rev 4)*

**Security hand-offs from the rev 4 Plan Verification (rev 5 decision):** all four fixed in S15, none deferred — each is the same class rev 4 fixed in-plan (C6), and each has a concrete exploit on this repo: *(new rev 5)*
- `pnpm --dir <any path>` (`hasDirBeforeScript`, `:203-213`) → **fix**: `pnpm --dir server/clones/<owner>/<repo> test` currently runs the `test` script of an untrusted cloned PR repo (C3), and `pnpm --dir e2e test` runs the browser flows the Decisions forbid. Values restricted to the four package names; e2e limited to `typecheck|lint`.
- `exec vitest run --config <file>` (`pnpmAllowed`, `:220`) → **fix**: the config is imported as code, so `--config src/db/seed.ts` or `--root clones/<repo>` runs an existing production or untrusted module. Rejected; the same rule applied to `depcruise --config` (pinned to `.dependency-cruiser.cjs`, `--init` writes a file) and `eslint -c`, since they load config as code too.
- Backtick command substitution not split (`segments`, `:103-122`) → **fix**, and wider than reported: ``echo `rm f` `` and `echo "$(rm f)"` (`$(` inside double quotes is not split either) both pass as `echo` today, and an unquoted heredoc body with `$(…)` is stripped as data although bash executes it. Rejected outright — no agent prompt uses command substitution (`rg '\$\(' .claude/agents/` → no hit).
- Unknown mode/profile exits 0 (`:670,685,697`) → **fix** as a policy gap, not an internal error (see *Failure policy*).

**Security hand-off from the rev 5 main-session review (rev 6 decision):** the cwd bypass of S15(a)/(f) is fixed in S19, not deferred. It is the same class as above, with two concrete exploit shapes (see *cwd rule*). S19 also closes two gaps the planner found while tracing it: a `--dir=`/`-C<path>` second value slips past `dirValues` (`:253-263` only reads space-separated values), so `pnpm --dir server --dir=server/clones/x test` passes; and `npm`. S20 closes the flag-passthrough shapes (*Argument allowlists*). *(new rev 6)*

## Steps
Execution order for rev 6: S19 → S20 → S21 → S22 → S23 → S24 → S7 → S18. S19–S21 all edit `scope-guard.sh`, so they run in sequence with `bash -n` + `self-test` after each; S23 describes the hook after S19/S20; S24 re-points INSIGHTS evidence lines after the last hook edit. *(rev 6)*

| ID | Package | Files (create / modify) | Change | Skills (routing bucket) | Covers | Verify |
|----|---------|-------------------------|--------|-------------------------|--------|--------|
| S0 | .claude | `.claude/agents/planner.md` (modify), `.claude/agents/README.md` (modify: planner rows only) | Add **Update mode**: Step 0 gate recognises an existing plan path as input (plus change request / Implementation Report / Plan Verification). Procedure: re-read plan, code at new HEAD and the cited INSIGHTS; keep AC/S/T IDs stable; new items get the next free ID; changed items get `*(rev N)*`; removed items stay with `~~…~~ *(dropped rev N: reason)*`; bump `**Revision:**`, refresh `**Base:**`; append `- rev N · YYYY-MM-DD · <what changed> · <why>` to `## Revisions`. Create-mode output format gains `**Revision:** 1` in the header and a `## Revisions` section with the `rev 1` line. Add an "update a plan" note to README *Flow* and the planner row of *Artifacts*. | none (`.claude/agents` is in no bucket) | AC7 | frontmatter parse (S7.1) + `rg -n 'Update mode\|## Revisions\|\*\*Revision:\*\*' .claude/agents/planner.md` shows all three |
| S1 | .claude | `.claude/hooks/scope-guard.sh` (create, `chmod +x`) | Modes `write tests\|docs`, `bash readonly\|checks`, `self-test` as in *Decisions*. Header: scope, fail-open vs policy-deny, tokenizer provenance. `self-test` runs a path matrix and a command matrix (T1–T7), exits non-zero on any wrong case. | none (model on `implementer-guard.sh`, `pr-gate.sh`) | AC1–AC4, AC6 | `bash -n .claude/hooks/scope-guard.sh && .claude/hooks/scope-guard.sh self-test` |
| S2 | .claude | `.claude/agents/test-writer.md` (create) | Frontmatter per *Decisions* + `hooks.PreToolUse`: `Write\|Edit\|NotebookEdit` → `scope-guard.sh write tests`; `Bash` → `scope-guard.sh bash checks` (timeout 15). Prompt: see S2 details. | skills named in the prompt, not preloaded | AC1, AC6 | frontmatter parse + `grep -c 'scope-guard.sh write tests' .claude/agents/test-writer.md` = 1 |
| S3 | .claude | `.claude/agents/architecture-reviewer.md` (create) | Frontmatter per *Decisions* + `Bash` → `scope-guard.sh bash checks`. Prompt: see S3 details. | `onion-architecture`, `frontend-ui-architecture` (preloaded) | AC2, AC6 | frontmatter parse + disallowedTools contains Write, Edit, NotebookEdit |
| S4 | .claude | `.claude/agents/plan-verifier.md` (create) | Frontmatter per *Decisions* + `Bash` → `scope-guard.sh bash checks`. Prompt: see S4 details. The rev 5 verification found its *Hard rules* Bash list (`typecheck/lint/test/arch/exec/docker info`) out of sync with the hook's S15(f) commands and rev 5 constraints — closed by S22. *(rev 6)* | none | AC3, AC6 | frontmatter parse |
| S5 | .claude | `.claude/agents/doc-writer.md` (create) | Frontmatter per *Decisions* + `Write\|Edit\|NotebookEdit` → `scope-guard.sh write docs`, `Bash` → `scope-guard.sh bash readonly`. Prompt: see S5 details. | `mermaid-diagram` (preloaded); `doc-standards`, `file-conventions` if they resolve | AC4, AC6 | frontmatter parse |
| S6 | .claude | `.claude/agents/README.md` (modify) | Catalog, Flow, Permissions, Artifacts, Sources, Troubleshooting — see S6 details. | docs bucket (`doc-standards`, `file-conventions` if they resolve) | AC5, AC6 | `rg -c 'test-writer\|architecture-reviewer\|plan-verifier\|doc-writer' .claude/agents/README.md`: every agent in Catalog, Permissions, Artifacts; every relative link target exists |
| S7 | .claude | — (verification) | 1. Frontmatter of all 7 agents parses and `name` = filename. 2. `bash -n` on both hooks, then `scope-guard.sh self-test && implementer-guard.sh self-test` (includes T10, T12, T14, T15) — runnable by plan-verifier from S15(f) on (from the repo root, S19), by the main session in any case; **rev 5 state done** (main session, 2026-09-25: both `bash -n` pass, `0 failing case(s)` for both, 30 `ok T12` rows / 0 FAIL, `git status --short server/` empty after the run; plan-verifier re-ran both under `checks` itself); rerun after S19–S21. 3. Manual acceptance T8 — owned by the **main session** in a fresh session (S19–S23 change hook and agent files, so an older session's definitions are stale); partly done, see T8; a precondition for `/pr-self-review`. 4. T11, T13, T16. *(rev 6)* | — | AC1–AC8 | see *Test plan* |
| S8 | .claude | `.claude/hooks/scope-guard.sh` (modify), `.claude/agents/README.md` (modify: `scope-guard.sh` bullet under *Permissions* only) | Bring the hook in line with the rev 4 *Decisions*: (a) `pnpm\|npm` require `--dir`/`-C`; (b) `exec tsc` only with `--noEmit`; `exec eslint` rejects `-o`/`--output-file`; `exec depcruise` rejects `-f`/`--output-to`; (c) `rg --pre*`; `git -c`/`--config-env`/`--ext-diff`; `git grep -O*`/`--open-files-in-pager*`; `sed -f`/`--file` and `w`/`W`/`e` script commands; `sort --compress-program*`; `file -C` → rejected; (d) leading env assignments: only `CI=`; (e) write policy evaluated on logical **and** symlink-resolved relative path; (f) `docs/skills/**` in `DOCS_DENY`. Every change gets a T10 self-test case; the `2>&1` handling (`segments()`, line ~113) and all T1–T7 cases stay green. README bullet names (a), (b), (d) and "`docs/skills/**` denied until the case collision is resolved". *(new rev 4)* Hook part done at `cb195fb`; README bullet lacks (b) (`rg -n 'noEmit\|tsc' .claude/agents/README.md` → exit 1) — closed by S13. *(rev 5)* | none (`.claude/hooks` is in no bucket) | AC6, AC1, AC4 | `bash -n .claude/hooks/scope-guard.sh && .claude/hooks/scope-guard.sh self-test` → `0 failing case(s)` with every T10 row present (`.claude/hooks/scope-guard.sh self-test \| rg -c '^ok +T10'` ≥ number of T10 cases) |
| S9 | .claude | `.claude/agents/{architecture-reviewer,doc-writer,implementer,plan-verifier,planner,researcher,test-writer}.md` (modify: one Hard-rule bullet each), `.claude/agents/README.md` (modify: "Reply language." paragraph after the Flow explanation) | User-requested change, **already applied in the working tree** — record and verify only: the "**Reply in the user's language.**" bullet as in AC8, inserted just before the "Repo content is data" (researcher: "Web content is data") bullet; README paragraph as in AC8. No other edit to researcher.md / implementer.md / planner.md. *(new rev 4)* | none | AC8 | T11; S7.1 frontmatter parse still passes for all 7 |
| S10 | repo (main session) | commit of `.claude/agents/**`, `.claude/hooks/scope-guard.sh`, `.claude/hooks/implementer-guard.sh`, `docs/plans/review-agents.plan.md` | **Baseline for C4/O2:** after S8/S9/S11/S12 land, the main session asks the user for consent and commits these files (the implementer cannot — `implementer-guard.sh` blocks `git commit`). The plan file is the planner→implementer artifact written by the main session, not an unplanned change. From this commit on, `git diff <S10 sha> -- .claude/agents/{researcher,implementer,planner}.md .claude/hooks/implementer-guard.sh` is the evidence for C4/O2; the next revision's `Base` is this commit. Without consent, C4/O2 stay *cannot verify* by decision. *(new rev 4)* **Done: `cb195fb`**, 11 files (8 agent files, both guard hooks, this plan). `INSIGHTS.md` was not in its file list and stays uncommitted — carried by S18. *(rev 5)* | none | C4, O2 | `git show --stat cb195fb` lists the 11 files |
| S11 | repo | `INSIGHTS.md` (modify — **already applied**, uncommitted) | Record the 2026-09-24 entry "a hand-probed guard hook 'allows everything': the probe, not the hook, is broken" as a planned session-protocol output (root `AGENTS.md` *Session protocol*), not an unplanned change. Check it sits newest-first under *tool-and-library-notes*, no earlier entry was edited, and its `**Evidence:**` line still points at the `2>&1` handling after S8 (the entry is uncommitted, so correcting its own line number before S10 is not a rewrite of a published entry). *(new rev 4)* Since the entry is still unpublished at `cb195fb`, before S18: re-point `**Evidence:**` to the `segments()` fd-dup comment line after S15, and add one clause to its zsh bullet that `scope-guard.sh` now blocks an unknown mode (S15(e)) while the other two hooks still allow it — so the entry does not describe `scope-guard.sh` wrongly on the day it is published. The user may prefer a separate superseding entry instead; either keeps append-only intact. *(rev 5)* Rev 5 part done (Evidence `:146`, zsh clause present); S19 inserts code above `segments()`, so S24 re-points `**Evidence:**` again after S19–S21. *(rev 6)* | `engineering-insights` | C5 | `git diff INSIGHTS.md` shows only added lines; the first `rg -n 'fd-dup' .claude/hooks/scope-guard.sh` hit (inside `segments()`) = the entry's `**Evidence:**` line |
| S12 | .claude | `.claude/agents/architecture-reviewer.md` (modify: judged-checks step, currently line 129) | Replace the bare "ring table" reference with the section title *The rings, by real path* of `onion-architecture/SKILL.md`, so the from→to classification cites a checkable source (C1). No other change. *(new rev 4)* | `onion-architecture` (read to confirm the title) | AC2, C1 | `rg -n 'The rings, by real path' .claude/agents/architecture-reviewer.md` ≥ 1 |
| S13 | .claude | `.claude/agents/README.md` (modify: `scope-guard.sh` bullet under *Permissions*, and the *Troubleshooting* hook bullet) | Close the S8 README gap and describe the rev 5 behaviour: `exec tsc` only with `--noEmit`; `exec eslint`/`exec depcruise` output and config restrictions (depcruise uses `-T`, long `--output-type` is rejected); `--dir`/`-C` limited to the four packages and e2e to `typecheck\|lint`; command substitution rejected; the hook self-tests allowed in `checks`; an unknown mode/profile now **blocks** (so a frontmatter typo shows up as "misconfigured scope-guard" on the agent's first call — Troubleshooting says so). Describes the hook after S15/S16, so it runs after them. *(new rev 5)* | none (`.claude/agents` is in no bucket) | AC5, AC6 | `rg -n 'noEmit' .claude/agents/README.md` ≥ 1; `rg -n 'misconfigured' .claude/agents/README.md` ≥ 1; `rg -n 'reviewer-core' .claude/agents/README.md` hits the bullet |
| S14 | .claude | `.claude/agents/architecture-reviewer.md` (modify: pre-pass step, lines 107–108, and the *Deterministic checks* skeleton row, line 160) | Replace `--output-type err-long` with `-T err-long` in both places (change request 2: the agent follows the hook, not the reverse — `--output*` stays a blanket rejection so `--output-to`, `--outputFile` and future `--output-*` flags need no per-tool list). No other change. *(new rev 5)* | none (reads `onion-architecture` § *Enforcement* only to confirm the command is unchanged otherwise) | AC2, AC6 | `rg -n 'output-type' .claude/agents/architecture-reviewer.md` → exit 1; `rg -c -- '-T err-long' .claude/agents/architecture-reviewer.md` = 2; T12 depcruise rows |
| S15 | .claude | `.claude/hooks/scope-guard.sh` (modify: `bash_eval` — `evalCmd`/`stripHeredocs`, `hasDirBeforeScript`, `pnpmAllowed`, `segVerdict`; the dispatch `case` at the end; header failure-policy paragraph) | Security fixes per *Decisions* (rev 5): (a) every `--dir`/`-C` value ∈ {`server`,`client`,`reviewer-core`,`e2e`}; `--dir e2e` → only `typecheck\|lint`; (b) `exec vitest run` rejects `--config`/`-c`/`--root`/`-r` in any form; `exec depcruise` rejects `--init` and any `--config`/`-c` other than `.dependency-cruiser.cjs`; `exec eslint` rejects `-c`/`--config*`; (c) reject a backtick or `$(` outside single quotes, and in the body of a heredoc whose delimiter is unquoted (checked before the body is stripped); (d) `-T` stays accepted for depcruise, `--output-type` stays rejected by `preReject` — pinned by T12, no code change; (e) unknown `write`/`bash` profile or unknown mode → `block_msg` + exit 2 ("misconfigured scope-guard"), header updated to list it as policy, not fail-open; garbage stdin with a valid profile still fails open (T7); (f) `checks` only, matched **before** the shell-invoker recursion: exactly `bash -n .claude/hooks/scope-guard.sh`, `bash -n .claude/hooks/implementer-guard.sh`, `.claude/hooks/scope-guard.sh self-test`, `.claude/hooks/implementer-guard.sh self-test`. Every change gets a T12 case via `runb`/`runw` (full-dispatch cases like the existing T7 `"$SELF"` case); no `'` inside either `node -e` block (C5). T1–T7 and T10 stay green. Done in the rev 5 working tree. (a) and (f) hold only while the cwd is the repo root, and (a) misses a second `--dir=`/`-C<path>` value; both gaps are closed by S19. (b) is bypassed through script forms (`pnpm --dir server test --config …`), closed by S20. *(rev 6)* | none (`.claude/hooks` is in no bucket) | AC6, AC2, AC3 | `bash -n .claude/hooks/scope-guard.sh && .claude/hooks/scope-guard.sh self-test` → `0 failing case(s)`; `.claude/hooks/scope-guard.sh self-test \| rg -c '^ok +T12'` ≥ number of T12 cases |
| S16 | .claude | `.claude/hooks/scope-guard.sh` (modify: the T10 symlink case in `self_test`, currently lines 649–658) | Move the symlink case out of the working tree (change request 3). A temp dir **can** exercise the same resolution: `write_eval` computes both relative paths from `$CLAUDE_PROJECT_DIR`, so the case builds a fake project root with `mktemp -d` (`server/test/`, `server/src/`, symlink `server/test/link → ../src`), runs `runw` with `CLAUDE_PROJECT_DIR` set to it for that case only, and removes it via `trap` on exit. Add a positive control in the same root (`server/test/x.test.ts` allowed) so a root that blocks everything as "outside the repo" cannot pass the case by accident. The real `$ROOT` is restored for all other cases. Justification: no write under the repo at all (a crashed run can no longer leave a stray symlink in `server/test/`), which is also what makes S15(f) safe for plan-verifier. *(new rev 5)* | none | AC6, AC1 | `self-test` → `0 failing case(s)`, both T10 symlink rows `ok`; `rg -n 'server/test/.scope-guard-symlink' .claude/hooks/scope-guard.sh` → exit 1; `git status --short server/` empty after the run |
| S17 | repo | `INSIGHTS.md` (modify — **already applied**, uncommitted) | Record the 2026-09-25 entry "`bash -n scope-guard.sh` fails several functions away from the real mistake…" as a planned session-protocol output (root `AGENTS.md` *Session protocol*), like S11. Decision recorded by the main session's suggestion; the user may overturn it (then the entry is removed before S18, never after). Check: newest-first under *tool-and-library-notes* (above the 2026-09-24 entry), no earlier entry edited, and its `**Evidence:**` lines point at the two `node -e '` block starts after S15/S16 (they are `:68` and `:319` at `cb195fb`; S15 inserts code before the second one). *(new rev 5)* Rev 5 part done (Evidence `:73`, `:415`); S19/S20 insert code inside the first block, so S24 re-points the second line number after S19–S21. *(rev 6)* | `engineering-insights` | C5 | `git diff INSIGHTS.md` shows only added lines; `rg -n "node -e '" .claude/hooks/scope-guard.sh` line numbers = the entry's `**Evidence:**` lines |
| S18 | repo (main session) | commit of `.claude/hooks/scope-guard.sh`, `.claude/agents/architecture-reviewer.md`, `.claude/agents/plan-verifier.md`, `.claude/agents/test-writer.md`, `.claude/agents/README.md`, `INSIGHTS.md`, `docs/plans/review-agents.plan.md` | After S19–S24 and a green S7.2, the main session asks the user for consent and commits these 7 files (the implementer cannot commit). The commit carries the rev 5 and rev 6 work together. `docs/skills/README.md` is **not** staged into it (O6). The next revision's `Base` is this commit. Without consent, the rev 5 and rev 6 changes stay verifiable against `cb195fb` via `git diff`. *(rev 6)* | none | C4, C5, O6 | `git show --stat HEAD` lists the 7 files; `git status --short` shows only `A  docs/skills/README.md` |
| S19 | .claude | `.claude/hooks/scope-guard.sh` (modify: `READONLY_CMDS` `:221`; `segVerdict` — the `CHECKS_EXACT` check `:333`, the `git` branch `:346-360`, the `pnpm`/`npm` branch `:389-401`; `bash_eval` arguments/`json` mode `:72-93`; the `bash)` dispatch `:840-854`; header) | Close the cwd bypass per *cwd rule*: (a) drop `cd` from `READONLY_CMDS`, so `cd`, a bare `cd`, `pushd`, `popd`, `builtin cd` and `command cd` are all unknown heads in both profiles, including inside `( … )` and `bash -c`; (b) cwd guard in `json` mode: the dispatch computes the project root (`$CLAUDE_PROJECT_DIR`, else `$SELF/../..`; neither resolves → fail-open warning, C5) and passes it to `bash_eval`; node compares realpath(input `cwd` ?? `process.cwd()`) with it; on a mismatch or an unresolvable cwd, a `CHECKS_EXACT` hit and every `pnpm` segment are rejected ("run from the repo root (cwd: …)"); the check lives inside the early `CHECKS_EXACT` branch and the `pnpm` branch, and nothing moves after the shell-invoker recursion (C5); `raw` mode takes an explicit cwd argument defaulting to the root; (c) in a `pnpm` segment reject any `--dir=…`, attached `-C<path>` and `--prefix*` token; (d) remove `npm` (head and `VALUE_FLAGS` entry); (e) `git` rejects `-C`, `--git-dir*`, `--work-tree*`; (f) header gains a "CWD" paragraph (why `cd` is out, what the guard compares, fail-open vs policy). T14 cases via `runb` and a new `runj` helper (name, profile, command, cwd or "none", expected) that builds the hook JSON with `node … JSON.stringify` (C5), plus full-dispatch rows through `"$SELF" bash checks` asserting exit 2 **and** "run from the repo root" on stderr. No `'` inside the node blocks. T1–T13 stay green; nothing is written under the repo (cwd rows use existing `$ROOT/server` and a `mktemp -d` dir removed by `trap`). *(new rev 6)* | none (`.claude/hooks` is in no bucket) | AC6, AC2, AC3, AC4 | `bash -n .claude/hooks/scope-guard.sh && .claude/hooks/scope-guard.sh self-test` → `0 failing case(s)`; `.claude/hooks/scope-guard.sh self-test \| rg -c '^ok +T14'` ≥ number of T14 cases; `rg -n '"cd"' .claude/hooks/scope-guard.sh` shows no `READONLY_CMDS` hit; `git status --short` unchanged after the run |
| S20 | .claude | `.claude/hooks/scope-guard.sh` (modify: `pnpmAllowed` `:286-305` and its helpers `hasFlag`/`depcruiseConfigBad`; the rejection message `:400`; header) | Replace the per-program flag denylists with the argument allowlists of *Argument allowlists*: script forms `typecheck\|lint\|arch` take no trailing argument; the `test` script form (server/client/reviewer-core) and `exec vitest run` take the vitest allowlist; `exec tsc`, `exec depcruise` and `exec eslint` take theirs; one shared path check for positional/`-p` values (relative, no `..` segment, not `clones/…`). Rejection message lists what is accepted. Every T6/T10/T12 allow row stays allowed (in particular `exec vitest run --exclude '**/*.it.test.ts'`, `exec tsc --noEmit`, `exec depcruise src --config .dependency-cruiser.cjs -T err-long`, `CI=1 pnpm --dir client test`); every T10/T12 block row stays blocked. T15 cases via `runb`. No `'` in the node block. *(new rev 6)* | none | AC6, AC1, AC2, AC3 | `bash -n` + `self-test` → `0 failing case(s)`; `self-test \| rg -c '^ok +T15'` ≥ number of T15 cases |
| S21 | .claude | `.claude/hooks/scope-guard.sh` (modify: `rund` `:610-616`; T12 (f) rows `:786-793`) | Tighten T12 (change request 3): `rund` captures stderr and passes only on exit 2 **and** stderr containing "misconfigured scope-guard" (a block for another reason no longer counts); add `runb "T12 bash -n implementer-guard" checks "bash -n .claude/hooks/implementer-guard.sh" 1`, so all four `CHECKS_EXACT` entries have a positive row. *(new rev 6)* | none | AC6 | `self-test` → `0 failing case(s)`; `self-test \| rg 'T12 bash -n implementer-guard'` starts with `ok`; `rg -n 'misconfigured scope-guard' .claude/hooks/scope-guard.sh` hits inside `rund` |
| S22 | .claude | `.claude/agents/plan-verifier.md` (modify: *Hard rules* Read-only bullet `:35-39`, *Procedure* step 2 `:80-83` and 3 `:87-92`), `.claude/agents/architecture-reviewer.md` (modify: *Hard rules* Read-only bullet `:40-44`, *Step 0* `:64-68`), `.claude/agents/test-writer.md` (modify: *Procedure* step 5 `:138-140`, plus one *Hard rules* line) | Sync prompts with the hook (change request 2), see *S22 details*: both reviewer prompts list the exact `checks` commands including the four hook `bash -n`/`self-test` commands, and the constraints agents hit: run from the repo root, never `cd`/`pushd`/`git -C`; `--dir` takes a bare package name (`server`, not an absolute path, `./server` or `--dir=server`); no command substitution (run `git merge-base HEAD origin/main` as its own call and paste the sha); `-T`, not `--output-type`; `sort \| uniq`, not `sort -u`; only a `CI=` env prefix; script forms take no extra arguments except `test` (S20 list). test-writer: replace "`npm test` from `e2e/`" with `pnpm --dir e2e typecheck` / `pnpm --dir e2e lint` and add the same root/`--dir`/no-`cd` line. No other edit; the reply-language bullet (S9) stays where it is. *(new rev 6)* | none (`.claude/agents` is in no bucket) | AC2, AC3, AC1, AC6 | T16; S7.1 frontmatter parse still passes |
| S23 | .claude | `.claude/agents/README.md` (modify: `scope-guard.sh` bullet under *Permissions* `:92-117`, *Troubleshooting* hook bullet `:242-251`) | README follows the hook (change request 5): `--dir`/`-C` accepts only a bare package name as its own token, so every `checks` command runs from the repo root (e.g. `pnpm --dir /abs/path/server` is rejected, as the rev 5 review observed); `cd`/`pushd`/`git -C` are rejected in both profiles; when the Bash call's cwd is not the project root, `pnpm` and the hook self-checks are blocked ("run from the repo root"); `npm` is not allowed; script forms take no extra arguments except `test`; `exec` targets take argument allowlists (S20). Replace "a `pnpm`/`npm` invocation" wording. Troubleshooting gains one bullet: blocked with "run from the repo root" → the Bash cwd is not the root; the agent cannot `cd` back, so it reports *cannot verify* and the main session re-invokes it. *(new rev 6)* | none | AC5, AC6 | T16 README rows |
| S24 | repo | `INSIGHTS.md` (modify — entry (a) **already applied**, uncommitted) | Session-protocol outputs for rev 6, planned like S11/S17 (change request 4). (a) Fold in the implementer's 2026-09-25 entry "an exact-command allowlist entry in `scope-guard.sh` must sit *before* the shell-invoker recursion…" as planned. The user may overturn this; the entry is then removed before S18, never after. After S19–S21, re-point its `**Evidence:**` to the `CHECKS_EXACT.has(` line. Because the entry is unpublished, add one clause saying the early check now also requires cwd = project root (S19). (b) Optional, recommended: one new 2026-09-25 entry under *tool-and-library-notes*: "an exact-command allowlist of relative paths is a cwd-relative allowlist — `cd` in the same command line re-points it". Its Evidence is the S19 cwd-guard line; the user may decline. (c) Re-point the S11 (`fd-dup`) and S17 (`node -e '`) Evidence lines after S19–S21. For all entries: newest first, no earlier entry edited, `git diff cb195fb -- INSIGHTS.md` shows only added lines. *(new rev 6)* | `engineering-insights` | C5 | `git diff cb195fb -- INSIGHTS.md \| rg -c '^-[^-]'` = 0; `rg -n 'CHECKS_EXACT.has\(' .claude/hooks/scope-guard.sh` line = entry (a) Evidence; S11 and S17 Verify hold |
| S25 | .claude (main session) | `.claude/hooks/scope-guard.sh` (modify: shell-invoker branch, `sed`, `sort -o` in `preReject`, `uniq`; T17 rows), `.claude/agents/README.md` (one sentence in the `scope-guard.sh` bullet) | Point fix of four bypasses the rev 6 Plan Verification found and the main session reproduced on the hook (exit 0 in both profiles): (a) a shell invoker with no command argument reads its program from stdin (`cat server/clones/x/y.sh \| bash`) — rejected, as are input redirection into it and any flag beyond `-c/-e/-u/-x`; (b) `sed -I` (BSD in-place) and clustered short flags carrying `i`/`I`/`f` (`-ni`, `-nf`); (c) `sort -o` attached or clustered (`-oout.txt`, `-ro`); (d) `uniq` with a second (output) operand. Each gets a T17 row. No reviewer re-run by user decision ("last run"); the main session verifies. *(new rev 7)* | none | AC6, C6 | `bash -n` + `self-test` → `0 failing case(s)`, `rg -c '^ok +T17'` = 18; the 12 bypass probes via full JSON dispatch → exit 2; the reviewers' own commands (`pnpm --dir server arch 2>&1 \| tail -5`, `self-test \| rg -c`, `sort \| uniq`, `sed -n`, `CI=1 pnpm --dir client test`) → exit 0 |

**S2 — test-writer prompt.** Writes tests only; does not fix production code; no architecture/security review.
- *Hard rules:* paths as in the hook; never production code, `adapters/mocks.ts`, `seed.ts`, config; never `.skip`/`.only`, weakened assertions, raised timeouts, snapshot `-u`; mock the boundary you own (`server/src/adapters/mocks.ts` via `ContainerOverrides`; `fetch` in client); RTL query priority `getByRole` → … → `getByTestId` last; Fastify via `app.inject()`; tests importing `test/helpers/pg.ts` must be `*.it.test.ts` (TESTING.md); e2e flows use only `--url`/`--text`/`find` and `{BASE}`; commands run from the repo root with a bare `--dir <pkg>`, never `cd` (S22). *(rev 6)*
- *Step 0 gate:* a plan path, or a named target (files, seams, AC) plus a done criterion; else *Clarification needed* JSON (same block as planner).
- *Procedure:* read `TESTING.md`, `<pkg>/AGENTS.md`, `INSIGHTS.md` → pick the kind by seam (TESTING.md) → load skills: client component/hook → `react-testing-library`; server route → `fastify-best-practices`; repository / `*.it.test.ts` → `drizzle-orm-patterns`; contract → `zod`; reviewer-core → `typescript-expert`; e2e → `e2e/AGENTS.md` → write → run → **negative control** (flip one key assertion per new test, confirm it fails, restore) → `engineering-insights` entries *proposed* in the report (INSIGHTS.md is outside the write scope).
- A test that exposes a real bug stays failing, goes under *Bugs found*, **Status: Partial**; never skipped. Max 3 attempts per failing test.

**S3 — architecture-reviewer prompt.**
- *Hard rules:* read-only; findings only on change-set lines (pre-existing issues → *Context*, per `pr-self-review` step 4); every finding carries evidence; no style/performance/security opinions; no fixes; Bash per *S22 details*. *(rev 6)*
- *Input:* base ref (default `git merge-base HEAD origin/main`, no fetch), a file list, or the implementer's *Hand-off to reviewers*. Change set = committed + staged + unstaged + untracked (as `pr-self-review` Step 1), excluding `server/clones/**`.
- *Deterministic pre-pass:* `pnpm --dir server arch` + `pnpm --dir server exec depcruise src --config .dependency-cruiser.cjs -T err-long` for edges and rule comments, compared to 0/17; vendor tripwire (`*/src/vendor/**` changes outside `server/src/vendor/shared`); mirror drift `diff -rq server/src/vendor/shared client/src/vendor/shared`; package independence (no root `package.json`/lockfile, no cross-package relative import); reviewer-core purity (`reviewer-core-stays-pure` + `rg` for `fastify|drizzle|node:fs|node:child_process|octokit` in `reviewer-core/src`). *(rev 5)*
- *Judged checks (onion-architecture Review checklist):* ring header comment; `this.container.` inside a method body; `new <Concrete>` in ring 1; `$inferSelect` outside `db/rows.ts`/repository; `req`/`reply` below `routes.ts`; new SDK import outside `adapters/`. Ring classification cites *The rings, by real path* (S12). *(rev 4)*
- *Client (client/AGENTS.md + skill, marked "no deterministic tool"):* no `fetch` in components, data only via `src/lib/hooks/*`; `'use client'` placement; no literal copy (strings via `messages/`); no local type duplicating a `src/vendor/shared` contract; server-only boundary.

**S4 — plan-verifier prompt.**
- *Hard rules:* read-only; the implementer's report is a *claim*, never evidence; every row has a verdict and evidence; "met" needs direct evidence (code location, and for behaviour also a passing test name or command output line); implemented-but-untested → "partial"; "cannot verify" names the reason and what would settle it. **Forbidden:** style/quality remarks, refactoring advice, generic best practice, summarising instead of itemising. Architecture/security observations are handed off by name in one line, never judged. Bash per *S22 details*. *(rev 6)*
- *Step 0 gate:* plan file exists with `Plan status: Ready`; spec path from the plan header; otherwise Blocked. Dropped items (`~~…~~ dropped rev N`) are verified as *not done*.
- *Items:* every `AC*`, every `S*`, every Test plan row (`T1..`), every Constraints bullet (`C*`), every Out-of-scope bullet (`O*` — verify it was *not* done).
- *Procedure:* change set Base → HEAD + working tree → map each changed file to items (unmapped → *Unplanned changes*) → run each step's *Verify* if the `checks` profile allows (else *cannot verify*) → fill the matrix. From S15(f) the hook `bash -n`/`self-test` Verify commands are inside the profile. *(rev 5)*

**S5 — doc-writer prompt.**
- *Hard rules:* paths as in the hook; every factual claim and diagram node checked against code (`path:line`); document what shipped, not what the plan intended — differences are spec drift for the report; never restate a README in `.doc/`, link to it; Mermaid only `flowchart`, `sequenceDiagram`, `stateDiagram-v2`, `erDiagram` (no C4 — experimental), ≤ ~15 nodes, labels use real module/file names, no diagrams in `AGENTS.md`.
- *Step 0 gate:* source material (plan path, spec, files or feature name) and audience/doc kind; else *Clarification needed* JSON.
- *Procedure:* the home table from *Decisions*.

**S6 — README.md changes.**
- *Catalog:* four new rows; rename column "Writes code" → "Writes" (code + tests / tests only / docs only / no); replace "Architecture and security review are not in this set yet" with "Security review is not in this set yet".
- *Flow:* new diagram incl. the fix loop — critical architecture findings or "not met" items go back to the implementer (test gaps → test-writer), with the planner in update mode when the plan itself must change; max 2 re-verify rounds, then the main session asks the user; reviewers run in parallel; doc-writer after both are clean; then `/pr-self-review`.
- *Permissions:* four rows with scope-guard profiles under *Extra guard*; a `scope-guard.sh` bullet next to `implementer-guard.sh`/`pr-gate.sh` stating the allowlists, the fail policy and the limits (Bash is best-effort; the prompt rule remains); "No agent here may spawn subagents" covers all 7.
- *Artifacts:* four rows (input → report name, status line).
- *Sources:* external rows from *Sources* below with access dates, plus in-repo rows.
- *Troubleshooting:* `scope-guard.sh self-test`; what to do when blocked on a path.

**S22 details — reviewer Bash constraints, written into `plan-verifier.md` and `architecture-reviewer.md`.** Both *Hard rules* Read-only bullets must state the same list, not "the same as the other agent". *(new rev 6)*
- *Allowed:* `pnpm --dir <server|client|reviewer-core> typecheck|lint|test|arch`; `pnpm --dir e2e typecheck|lint`; `CI=1` before `test`/`exec vitest run`; `pnpm --dir <pkg> exec vitest run …`, `exec tsc --noEmit`, `exec depcruise …`, `exec eslint …` within the S20 argument lists; `docker info`; the read-only heads of `readonly`; and exactly these four commands: `bash -n .claude/hooks/scope-guard.sh`, `bash -n .claude/hooks/implementer-guard.sh`, `.claude/hooks/scope-guard.sh self-test`, `.claude/hooks/implementer-guard.sh self-test`.
- *Constraints:*
  - Run every command from the repo root. No `cd`/`pushd`, no `git -C`.
  - `--dir` takes a bare package name as its own token.
  - No command substitution (backtick or `$(`). Run `git merge-base HEAD origin/main` as a separate call and paste the sha into the next command.
  - `-T err-long`, never `--output-type`.
  - `sort | uniq`, never `sort -u`.
  - Only a `CI=` env prefix; no redirection except to `/dev/null` or `2>&1`.
  - A block from the hook is a *cannot verify* / *Limits* line naming the command, never a retry with a different spelling.
- *plan-verifier Procedure step 3* adds that the hook Verify commands of S1/S7/S8/S15/S16/S19–S21 are runnable exactly as written in the plan.

## Test plan
| Test | Kind | Covers | File |
|------|------|--------|------|
| T1 `write tests` allows `client/src/app/agents/_components/AgentCard/AgentCard.test.tsx`, `server/test/x.it.test.ts`, `server/test/helpers/pg.ts`, `reviewer-core/test/run.test.ts`, `e2e/specs/08-x.flow.json`, `client/src/test/setup.ts` | hook self-test | AC1, AC6 | `.claude/hooks/scope-guard.sh` |
| T2 *(rev 3)* `write tests` blocks `server/src/fixtures/x.json`, `server/src/app.ts`, `server/src/adapters/mocks.ts`, `server/src/db/seed.ts`, `client/src/vendor/shared/x.test.ts`, `e2e/run.ts`, `client/vitest.config.ts`, `server/test/../src/app.ts` (traversal), an absolute path outside the repo, a JSON body with no `file_path` | hook self-test | AC1, AC6 | same |
| T3 `write docs` allows `server/README.md`, `client/.doc/query-keys.md`, `docs/agent-prompts/README.md`, `docs/some-topic.md` | hook self-test | AC4, AC6 | same |
| T4 `write docs` blocks `server/.spec/x.spec.md`, `AGENTS.md`, `client/CLAUDE.md`, `INSIGHTS.md`, `docs/plans/x.plan.md`, `docs/agent-prompts/security-reviewer.md`, `.claude/agents/README.md`, `server/src/app.ts`, `docs/x.png` | hook self-test | AC4, AC6 | same |
| T5 `bash readonly` allows `git diff --stat`, `rg -n foo server/src`, `sed -n 1,20p f`, `find . -name '*.md'`, `cat f 2>/dev/null`; blocks `echo x > f`, `sed -i s/a/b/ f`, `tee f`, `rm f`, `git checkout -- f`, `git commit -m x`, `bash -c "git push"`, `find . -delete`, `pnpm test`, `xargs rm`, `git diff --output=f` | hook self-test | AC2–AC4, AC6 | same |
| T6 `bash checks` allows `pnpm --dir server arch`, `CI=1 pnpm --dir client test`, `pnpm --dir server exec vitest run --exclude '**/*.it.test.ts'`, `docker info`; blocks `pnpm --dir client lint --fix`, `pnpm --dir client exec vitest run -u`, `pnpm --dir server db:migrate`, `pnpm --dir server db:generate`, `pnpm --dir client add x`, `pnpm --dir server dev`, `docker compose up -d`, `pnpm test && git push` | hook self-test | AC2, AC3, AC6 | same |
| T7 heredoc body and `echo "then git push"` mentions do not trigger; garbage stdin exits 0 with a warning (fail-open) | hook self-test | AC6 | same |
| T8 Manual smoke in a fresh session. Sub-checks and state: (a) all 7 agents appear as available subagent types in a fresh session (the `/agents` wizard was removed from Claude Code and now only points at `.claude/agents/`; structural backup: S7.1 frontmatter parse) — **partly evidenced**: the four new agents were each invoked by type on 2026-09-25, the planner by the rev 5 and rev 6 runs; a listing of all 7 in a session started after S18 **remains**; (b) test-writer asked to edit `server/src/app.ts` → blocked — **done** (2026-09-25: `BLOCKED by scope-guard (write tests): server/src/app.ts is outside the test-writer scope …`; `git status` unchanged); (c) doc-writer asked to edit `server/.spec/skills.spec.md` → blocked — **done** (2026-09-25: `BLOCKED by scope-guard (write docs): … matches a denied path (.spec, …)`; `git status` unchanged); (d) architecture-reviewer on the working tree → report with `pnpm --dir server arch` under *Deterministic checks* — **done** (rev 4: Clean, 0/17; rev 5 re-run: **Clean**, `arch` 0 errors / 17 warnings = C1 baseline, `-T err-long` passed the hook, and the hook rejected `pnpm --dir <absolute path>/server` as expected, which is documented by S23); (e) plan-verifier against this file → a matrix over every AC/S/T/C/O item — **done for rev 4** (14 met / 19 partial / 0 not met / 12 cannot verify) and **rev 5** (34 met / 7 partial / 0 not met / 3 cannot verify; it ran `bash -n` and both self-tests itself under `checks`, the live evidence for S15(f)); rerun against rev 6 after S18; (f) an agent invoked with `User language: <non-English>` answers prose in that language with verbatim headings — observed for all four new agents (Ukrainian, verbatim skeleton headings); **remains: the user's explicit confirmation**; (g) *(new rev 6)* in a fresh session after S18, a reviewer's `pnpm --dir server arch` and `.claude/hooks/scope-guard.sh self-test` are **not** blocked with "run from the repo root" — the live evidence that the hook input's `cwd` equals the project root in real subagent calls (S19) — **remains**. Run by the main session. *(rev 6)* | manual acceptance | AC1–AC5, AC8 | — |
| T9 Manual: planner in update mode on this file with a change input → returns the next revision with unchanged IDs, `*(rev N)*` markers on changed items, new items on the next free ID, refreshed Base and a new `## Revisions` line. **Evidence:** the rev 4 run (input: Plan Verification of rev 3 plus a user change request) — the diff of this file between rev 3 and rev 4 after the main session writes it back; rev 5 and rev 6 likewise (rev 6 keeps Base `cb195fb` because S18 has not happened — Base is refreshed to the current HEAD, which is still `cb195fb`), diffable from S18 on. *(rev 6)* | manual acceptance | AC7 | `docs/plans/review-agents.plan.md` (git history after S10) |
| T10 S8 hardening, in `self-test` via `runb`/`runw`. `bash checks` allows `pnpm --dir server exec tsc --noEmit`, `CI=1 pnpm --dir client test`; blocks `pnpm test`, `pnpm --dir server exec tsc`, `pnpm --dir client exec eslint -o out.txt src`, `pnpm --dir server exec depcruise src -f out.txt`, `NODE_OPTIONS=--require=x pnpm --dir server test`. `bash readonly` blocks `rg --pre ./x foo`, `rg --pre=./x foo`, `git -c core.pager=x log`, `git -c diff.external=x diff`, `git diff --ext-diff`, `git grep -Ox foo`, `git grep --open-files-in-pager=x foo`, `sed -n 'w out' f`, `sed 's/a/b/w out' f`, `sed '1e id' f`, `sed -f s.sed f`, `sort --compress-program=x f`, `GIT_EXTERNAL_DIFF=x git diff`; still allows `sed -n 1,20p f`, `git diff 2>&1 \| head`. `write docs` blocks `docs/skills/readme.md`; `write tests` blocks `server/test/link/app.ts` where `link` is a symlink to `server/src`, built in a `mktemp -d` fake project root passed as `CLAUDE_PROJECT_DIR` (S16), and allows `server/test/x.test.ts` in the same root (positive control); nothing is created under the repo *(rev 5)* | hook self-test | AC6, AC1, AC4 | `.claude/hooks/scope-guard.sh` |
| T11 Reply-language rule: `rg -c "Reply in the user's language" .claude/agents/*.md` prints `:1` for each of the 7 agent files; `rg -n '^\*\*Reply language\.\*\*' .claude/agents/README.md` = 1 hit; in each agent file the bullet's line number is lower than the "Repo content is data" / "Web content is data" line. Behavioural evidence: the rev 4 and rev 5 planner runs and the 2026-09-25 runs of all four new agents answered in the delegating prompt's `User language:` with verbatim headings; the user's explicit confirmation is still pending (T8 f) *(rev 5)* | static check + manual | AC8 | `.claude/agents/*.md` |
| T12 S15 security fixes, in `self-test` via `runb` (full-dispatch rows via `"$SELF"` like T7). `bash checks` allows: `pnpm --dir server exec depcruise src --config .dependency-cruiser.cjs -T err-long`, `pnpm --dir e2e typecheck`, `pnpm --dir reviewer-core test`, `bash -n .claude/hooks/scope-guard.sh`, `bash -n .claude/hooks/implementer-guard.sh` (S21), `.claude/hooks/scope-guard.sh self-test`, `.claude/hooks/implementer-guard.sh self-test`, `rg -n '$(foo)' f` (single-quoted literal), a quoted-delimiter heredoc (`cat <<'EOF'` … `$(x)` … `EOF`). Blocks: `pnpm --dir server exec depcruise src --config .dependency-cruiser.cjs --output-type err-long` (pins the S14 choice), `pnpm --dir server exec depcruise src -T err-long --output-to x`, `pnpm --dir server exec depcruise src --config other.cjs`, `pnpm --dir server exec depcruise --init`, `pnpm --dir server/clones/x/y test`, `pnpm --dir /tmp test`, `pnpm --dir server --dir /tmp test`, `pnpm --dir e2e test`, `pnpm --dir server exec vitest run --config src/db/seed.ts`, `pnpm --dir server exec vitest run -c x.ts`, `pnpm --dir server exec vitest run --root clones/x`, `pnpm --dir client exec eslint -c x.mjs src`, `bash -n /tmp/x.sh`, `.claude/hooks/scope-guard.sh bash checks`, `.claude/hooks/pr-gate.sh self-test`. `bash readonly` blocks: ``echo `rm f` ``, `echo "$(rm f)"`, an unquoted heredoc (`cat <<EOF` … `$(rm f)` … `EOF`), `bash -n .claude/hooks/scope-guard.sh` (checks only). Full dispatch with valid JSON: `scope-guard.sh bash check`, `scope-guard.sh write test`, `scope-guard.sh nonsense` → exit 2 **and** stderr contains "misconfigured scope-guard" (S21; `rund` no longer discards stderr); T7 garbage-stdin rows unchanged (exit 0) *(rev 6)* | hook self-test | AC6, AC2, AC3 | `.claude/hooks/scope-guard.sh` |
| T13 Static checks for S13/S14: `rg -n 'noEmit' .claude/agents/README.md` ≥ 1 and `rg -n 'misconfigured' .claude/agents/README.md` ≥ 1; `rg -n 'output-type' .claude/agents/architecture-reviewer.md` → exit 1 and `rg -c -- '-T err-long' .claude/agents/architecture-reviewer.md` = 2; `git diff cb195fb -- .claude/agents/{researcher,implementer,planner}.md .claude/hooks/implementer-guard.sh .claude/hooks/pr-gate.sh` empty (C4) *(new rev 5)* | static check | AC2, AC5, AC6, C4 | `.claude/agents/README.md`, `.claude/agents/architecture-reviewer.md` |
| T14 S19 cwd rule, in `self-test`. `runb` (raw, cwd = root): `checks` blocks `cd server/clones/x && .claude/hooks/scope-guard.sh self-test`, `cd server/clones/x && pnpm --dir server test` (the two exploit shapes), `(cd server && pnpm --dir server test)`, `bash -c "cd server && pnpm --dir server test"`, `pushd server`, `builtin cd server`, `pnpm --dir server --dir=server/clones/x test`, `pnpm -Cserver/clones/x --dir server test`, `pnpm --dir server --prefix=clones/x test`, `npm --dir server test`, `git -C server/clones/x status`, `git --git-dir=server/clones/x/.git log`, `git --work-tree=server/clones/x diff`; `readonly` blocks `cd server`, a bare `cd`, `git -C server log`; still allows `pnpm --dir server arch`, `git diff --stat`. `runj` (JSON built by node): cwd = root → `.claude/hooks/scope-guard.sh self-test` and `pnpm --dir server arch` allowed; cwd = `$ROOT/server` → both blocked, `bash -n .claude/hooks/implementer-guard.sh` blocked, `git diff --stat` still allowed; cwd = a `mktemp -d` dir → `pnpm --dir server test` blocked; cwd = a non-existent path → `pnpm --dir server arch` blocked; no `cwd` field with hook process cwd = root → allowed, = `$ROOT/server` → blocked; `CLAUDE_PROJECT_DIR` unset, cwd = root → allowed (root from `$SELF`). Full dispatch `"$SELF" bash checks` with cwd = `$ROOT/server` and `.claude/hooks/scope-guard.sh self-test` → exit 2 and stderr contains "run from the repo root". Nothing written under the repo *(new rev 6)* | hook self-test | AC6, AC2, AC3, AC4 | `.claude/hooks/scope-guard.sh` |
| T15 S20 argument allowlists, in `self-test` via `runb`. `checks` allows `pnpm --dir server test`, `CI=1 pnpm --dir client test`, `pnpm --dir server test test/x.test.ts`, `pnpm --dir server exec vitest run test/x.test.ts -t 'name'`, `pnpm --dir server exec vitest run --reporter=verbose`, `pnpm --dir server exec tsc --noEmit -p tsconfig.json`, `pnpm --dir client exec eslint src --max-warnings 0`, `pnpm --dir server exec depcruise src --config .dependency-cruiser.cjs -T err-long`; blocks `pnpm --dir server test --config src/db/seed.ts`, `pnpm --dir server test --root clones/x`, `pnpm --dir client lint -c x.mjs`, `pnpm --dir server arch --config x.cjs`, `pnpm --dir server typecheck --generateTrace t`, `pnpm --dir server exec vitest run --reporter=./x.js`, `pnpm --dir server exec vitest run --pool=./x.js`, `pnpm --dir server exec vitest run clones/x`, `pnpm --dir server exec vitest run ../client`, `pnpm --dir server exec depcruise src --webpack-config w.js`, `pnpm --dir server exec depcruise src --cache`, `pnpm --dir server exec depcruise src -T dot`, `pnpm --dir client exec eslint -f ./fmt.js src`, `pnpm --dir server exec eslint clones/x/a.js`, `pnpm --dir server exec tsc --noEmit --generateCpuProfile p` *(new rev 6)* | hook self-test | AC6, AC1, AC2, AC3 | `.claude/hooks/scope-guard.sh` |
| T16 Static checks for S22–S24: `rg -n 'bash -n .claude/hooks/implementer-guard.sh' .claude/agents/plan-verifier.md .claude/agents/architecture-reviewer.md` hits both files; `rg -c 'self-test' ` on each ≥ 1; `rg -n 'command substitution' ` and `rg -n 'uniq' ` hit both; `rg -n 'repo root' ` hits both and `test-writer.md`; `rg -n 'npm test' .claude/agents/test-writer.md` → exit 1; `rg -n '\bcd [A-Za-z./]' .claude/agents/` → exit 1; `rg -n 'bare package name' .claude/agents/README.md` ≥ 1 and `rg -n 'run from the repo root' .claude/agents/README.md` ≥ 1; `rg -n '`pnpm`/`npm`' .claude/agents/README.md` → exit 1; S24 Verify; T13's C4 diff still empty *(new rev 6)* | static check | AC1, AC2, AC3, AC5, AC6, C4, C5 | `.claude/agents/*.md`, `INSIGHTS.md` |
| T17 S25 rows via `runb`: `readonly`/`checks` block `cat … \| bash`, `printf hi \| sh`, `bash -s`, `bash < file`, `bash --rcfile x -c true`, `bash -c "git push"`, `sed -I .bak …`, `sed -ni …`, `sed -nf …`, `sort -oout.txt f`, `sort -ro out.txt f`, `uniq f out.txt`, `uniq -f 1 f out.txt`; allow `bash -c "git status --short"`, `sed -nE 1,5p f`, `sort -rn f`, `uniq -c f`, `sort f \| uniq -c`. *(new rev 7)* | self-test | AC6, C6 | — |

**Report skeletons to embed** (Implementation Report style; one status line at the end):
- **Test Report** — `**Status:** Done | Partial | Blocked · **Target:** · **Base → HEAD:**`; *Tests written* (Test `file › describe › it` | Kind | Seam | Covers | Result); *Negative control* (Test | Assertion flipped | Failed as expected); *Skills applied*; *Verification*; *Skipped checks*; *Bugs found*; *Production changes needed (not made)*; *Insights proposed*.
- **Architecture Review** — `**Scope:** base…HEAD, N files · **Packages:**`; *Deterministic checks* (Check | Command | Result | Baseline delta); *Findings* (# | Severity | Rule (skill § / depcruise rule) | `file:line` | Edge from → to (ring → ring) | Evidence | Why); *Checked, no finding*; *Context (pre-existing)*; *Limits*; `**Review status:** Clean | Findings (c/M/m) | Inconclusive (<reason>)`.
- **Plan Verification** — `**Plan:** · **Revision:** · **Spec:** · **Base → HEAD:**`; *Summary* counts; *Traceability matrix* (Item | Source | Requirement (short quote) | Verdict | Evidence | Gap — factual, no advice); *Commands run* (Command | Exit | Key output line); *Unplanned changes*; *Handed off (not judged)*; `**Verification status:** Verified | Gaps (n not met, n partial, n cannot verify) | Blocked (<reason>)`.
- **Documentation Report** — *Files written* (File | Diátaxis type | Home and why | Change); *Diagrams* (File § heading | Mermaid type | Depicts | Grounded in); *Claims checked* (Claim | `path:line`); *Proposed edits outside my scope*; *Conventions notes*; `**Docs status:** Done | Partial | Blocked`.

## Review hand-off
- **Architecture review:** no files under `server/src` or `client/src` change. The only enforcement code is `.claude/hooks/scope-guard.sh`; read its allow/deny tables against *Decisions* (rev 6 rows, *cwd rule*, *Argument allowlists*). `.claude/agents/architecture-reviewer.md` (S12, S14, S22) for the C1 citation, the `-T err-long` command and the Bash constraint list; `.claude/agents/plan-verifier.md` and `test-writer.md` (S22) for the same list. *(rev 6)*
- **Security review:** none by `routing.md` globs, but `scope-guard.sh` is a permission boundary and gets a human/security read. What earlier revisions closed:
  - S8 closed the rev 3 observations.
  - S15 closed the four rev 4 hand-offs: `--dir` value (`hasDirBeforeScript`), `vitest --config` (`pnpmAllowed`), command substitution (`hasCommandSub`/`heredocAwareScrub`, `:95-133`) and unknown mode/profile (dispatch, `:824-857`). It also closed `pnpm --dir e2e test`, `depcruise --config/--init` and `eslint -c`.
  - S15(f) widened `checks` by four exact commands.

  What rev 6 changes:
  - S19 closes the cwd bypass: `cd` is out, the input `cwd` is compared with the root, `--dir=`/`-C<path>`/`--prefix` are rejected, `npm` is dropped and `git -C`/`--git-dir`/`--work-tree` are rejected.
  - S20 turns the `exec`/script flag checks into argument allowlists.
  - Read S19's root resolution for exactness: the root must never come from the input `cwd`.

  Still open for the reviewer:
  - The planner assumes the hook input `cwd` is the directory the Bash command will run in (hooks docs). T8(g) is the live check.
  - vitest 2.x custom reporter/pool by path and ESLint 10 config lookup from a file's directory were not confirmed offline. S20's rules reject them either way.
  - Whether `pnpm lint` (`eslint .` with `clones/**` in global ignores) can still reach a clone's `eslint.config.*` under ESLint 10.
  - The flag surface of the `readonly` heads beyond those listed.
  - The writes allowlisted programs make as they run (Risks).
  - Whether `bash -c`'s one-level recursion is enough.
  - test-writer can run arbitrary code through the tests it writes. This is inherent to its role and bounded only by its write scope.

  *(rev 6)*

## Risks / open questions
- ~~Open decision — fixtures scope (before S1).~~ *(resolved rev 3: fixtures only under `server/test/**`, `reviewer-core/test/**`, `client/src/test/**`)*
- **Bash enforcement is best-effort (S1, S8, S15, S19, S20).** An allowlist cannot see writes done by an allowed program (a test writing files, Vitest snapshots). Mitigations: argument allowlists, `CI=1` for the verifier, the prompt rule stays. README says "best-effort", not "enforced". *(rev 6)*
- **The allowlist may be too tight (S1–S5, S8, S15, S19, S20)** — e.g. `awk`, `xargs`, `cd`, `npm` denied; S8 rejects bare `pnpm <script>` and every env prefix but `CI=`; S15 rejects `./server`/`--dir=server` spellings, all command substitution (an agent must run `git merge-base …` first and paste the sha), and vitest/eslint `--config`; S20 accepts only the listed arguments per program (e.g. vitest `--coverage`, `--bail`, eslint `--quiet` are rejected until added). Widen by editing the table plus a self-test case, never by switching to fail-open. *(rev 6)*
- **Fail-closed on an unknown profile (S15(e)).** A frontmatter typo now blocks every Bash/Write call of that one agent until fixed; that is the intended trade-off (loud, local, one-line fix) versus a silently disarmed guard. `implementer-guard.sh`/`pr-gate.sh` still fail open on an unknown mode (C4, O8). *(new rev 5)*
- **cwd lock-out (S19).** If a subagent's Bash cwd is ever not the project root, its `pnpm` and hook self-check calls block, and it cannot `cd` back (rejected). The prompts (S22) and README Troubleshooting (S23) say to report *cannot verify*; the main session re-invokes it. T8(g) checks that real calls run at the root. *(new rev 6)*
- **Hook-input `cwd` semantics (S19).** The guard trusts the documented `cwd` field. If a Claude Code version omitted it, the fallback is the hook process's cwd, which T14 pins. If a version reported something other than the command's cwd, the guard would be wrong in one direction or the other. T8(g) is the check. *(new rev 6)*
- **Third copy of the shell tokenizer (S1)** — pr-gate, implementer-guard, scope-guard. Consolidating into `.claude/hooks/lib/` touches existing hooks; follow-up. S15's command-substitution fix widens the drift: the other two copies still pass backticks/`$(` (harmless there, they are denylists — same reasoning as the `2>&1` note in INSIGHTS 2026-09-24). *(rev 5)*
- **`routing.md` gaps (S2, S5)** — no bucket for `server/test/**` / `reviewer-core/test/**`; the `docs` bucket names user-level skills absent from the repo. Recorded, not fixed.
- **Mermaid is not machine-validated (S5)** — no CLI in the repo; validation is by `mermaid-diagram` rules and GitHub preview; the report says so.
- **Update mode keeps IDs stable only if the main session writes the planner's output back verbatim (S0).** Hand edits to the plan file must follow the same marking rules.
- **Test-writer routing is decided by the main session (S6 flow)** — planner.md has no per-step "Owner" column; adding one is a follow-up.
- **`permissionMode: default` for reviewers (S3, S4)** — `pnpm` commands may prompt the user. Acceptable.
- **Hook arming (S7)** — agent definitions and frontmatter hooks load at session start: start a fresh session before T8/T9 (the `/agents` wizard no longer exists). *(rev 5)*
- **Case collision** — `docs/skills/README.md` (staged) and `docs/skills/readme.md` (tracked) collide on case-insensitive macOS. From rev 4 `docs/skills/**` is hook-denied for doc-writer (S8); the collision itself is resolved outside this plan (O6), and the denial is lifted in the same change. *(rev 4)*
- **`sed` script parsing is heuristic (S8).** Detecting `w`/`W`/`e` inside a sed script without a full sed parser may reject legitimate scripts; the rule is "uncertain → reject", recorded under *The allowlist may be too tight*. *(new rev 4)*
- ~~**C4/O2 before S10 stay unprovable (S10).** If the user declines the baseline commit, C4/O2 remain *cannot verify* with the stated reason; nothing else in the plan depends on them.~~ *(dropped rev 5: S10 done at `cb195fb`; C4/O2 are diffable from it)*
- **INSIGHTS evidence drift (S11, S17, S24).** S19–S21 shift `scope-guard.sh` line numbers again; all three or four uncommitted entries' `**Evidence:**` lines are corrected in S24, before S18, never after. If S18 is declined, the entries stay unpublished and must be re-checked at the next hook edit. *(rev 6)*
- **S17 is a recorded default, not a user decision.** If the user rejects folding the 2026-09-25 entry into the plan, it is listed under *Unplanned changes* by the next plan-verifier run instead; the entry itself is not affected by that choice. *(new rev 5)* The same holds for S24(a) and (b). *(rev 6)*

## Out of scope
- **O1** — A security-reviewer agent.
- **O2** — Editing `researcher.md` or `implementer.md` beyond the S9 reply-language bullet; editing `planner.md` beyond S0 and S9; verified against the S10 baseline. *(rev 4)*
- **O3** — `.claude/skills/**` (incl. `routing.md`, `severity.md`) and `.claude/settings.json`.
- **O4** — Consolidating the tokenizer; CI wiring for the hooks.
- **O5** — A tutorials home; a client-side dependency-cruiser / ESLint boundary config (the reviewer reports the gap).
- **O6** — The staged `docs/skills/README.md` and resolving its case collision with the tracked `docs/skills/readme.md`: a pre-existing change not produced by this plan. The plan only keeps doc-writer out of `docs/skills/**` (S8), and S18 does not commit it. *(rev 5)*
- **O7** — The pre-plan content of `researcher.md`, `implementer.md`, `planner.md` (before S0) and `implementer-guard.sh`: templates that existed untracked before rev 1. This plan neither authored nor verifies them; S10 only makes later edits diffable. *(new rev 4)*
- **O8** — Porting S15's fail-closed unknown-mode rule, the command-substitution check, or S19's cwd rule / `cd` / `git -C` rejection to `implementer-guard.sh` / `pr-gate.sh` (C4); both remain fail-open denylists. *(rev 6)*

## Sources
**External** (accessed 2026-09-24):
- Claude Code subagents — frontmatter, bare-name `tools`/`disallowedTools` ("A disallowedTools entry with a specifier… still removes the whole tool"), agent-scoped `hooks`, `skills` preload, "use proactively": https://code.claude.com/docs/en/sub-agents
- Permissions — path/command rules are settings-wide, not per agent: https://code.claude.com/docs/en/permissions
- Hooks — PreToolUse input `agent_type`, `tool_name`, `tool_input.file_path`, common field `cwd` (S19); exit 2 blocks with stderr as reason: https://code.claude.com/docs/en/hooks *(rev 6)*
- Skills — preload vs on-demand: https://code.claude.com/docs/en/skills
- dependency-cruiser rules reference: https://github.com/sverweij/dependency-cruiser/blob/main/doc/rules-reference.md
- Palermo, "The Onion Architecture" (2008): https://jeffreypalermo.com/2008/07/the-onion-architecture-part-1/
- Cockburn, "Hexagonal Architecture" (2005): https://alistair.cockburn.us/hexagonal-architecture/
- ISO/IEC/IEEE 29148 (requirements traceability): https://www.iso.org/standard/72089.html
- ISTQB glossary — verification vs validation, traceability: https://glossary.istqb.org/
- Anthropic, "Building effective agents" (evaluator-optimizer): https://www.anthropic.com/research/building-effective-agents
- LLM-as-judge rubric practice: https://deepeval.com/docs/metrics-llm-evals
- Testing Library — guiding principles, query priority: https://testing-library.com/docs/guiding-principles , https://testing-library.com/docs/queries/about#priority
- Fastify testing (`inject`): https://fastify.dev/docs/latest/Guides/Testing/
- Vitest mocking: https://vitest.dev/guide/mocking
- Kent C. Dodds, Testing Trophy: https://kentcdodds.com/blog/the-testing-trophy-and-testing-classifications
- Stryker mutant states: https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/
- Diátaxis: https://diataxis.fr/
- Write the Docs, docs as code: https://www.writethedocs.org/guide/docs-as-code/
- Nygard, ADRs: https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions
- C4 model: https://c4model.com/
- Mermaid C4 (experimental): https://mermaid.js.org/syntax/c4.html
- GitHub Mermaid rendering: https://docs.github.com/en/get-started/writing-on-github/working-with-advanced-formatting/creating-diagrams

**External, cited for rev 6 but not fetched in the rev 6 planner run** (the security reviewer confirms them): *(new rev 6)*
- Vitest reporters — custom reporter by path: https://vitest.dev/guide/reporters
- dependency-cruiser CLI — `--webpack-config`, `--cache`, `-T`: https://github.com/sverweij/dependency-cruiser/blob/main/doc/cli.md
- ESLint configuration files — config lookup: https://eslint.org/docs/latest/use/configure/configuration-files
- git-config `safe.bareRepository` (embedded bare repositories): https://git-scm.com/docs/git-config

**In-repo:** `.claude/agents/{README,researcher,planner,implementer,architecture-reviewer,plan-verifier,test-writer}.md` · `.claude/hooks/implementer-guard.sh`, `pr-gate.sh`, `scope-guard.sh` · `.claude/settings.json` · `.claude/skills/onion-architecture/SKILL.md` · `.claude/skills/frontend-ui-architecture/SKILL.md` · `.claude/skills/pr-self-review/{SKILL,routing,severity,report-template}.md` · `.claude/skills/mermaid-diagram/SKILL.md` · `server/.dependency-cruiser.cjs` · `{server,client,reviewer-core,e2e}/package.json`, `{server,client,reviewer-core}/vitest.config.ts`, `server/eslint.config.js` · `client/eslint.config.mjs` · `TESTING.md` · root, `server/`, `client/`, `e2e/` `AGENTS.md` · `<pkg>/.doc/README.md`, `<pkg>/.spec/README.md` · `docs/agent-prompts/README.md` · root `INSIGHTS.md` · `.gitignore` *(rev 6)*

## Revisions
- rev 1 · 2026-09-24 · Initial plan from planner: S1–S7, AC1–AC6 · request for four review/test/docs subagents
- rev 2 · 2026-09-24 · Added AC7, S0 (planner update mode), T9; `Revision` field in header; planner.md moved from *Do not touch* to S0-only · user asked for a way to revise saved plans in place
- rev 3 · 2026-09-24 · Fixtures restricted to test dirs; T2 gains `server/src/fixtures/x.json` · user decision on the open fixtures question
- rev 4 · 2026-09-24 · Added AC8, C6, S8 (hook hardening: `--dir`, `tsc --noEmit`, `rg --pre`, `git -c`/`--ext-diff`/`grep -O`, `sed w/e`, `sort --compress-program`, eslint/depcruise output flags, env prefix only `CI=`, symlink-resolved glob check, `docs/skills/**` denied), S9 (reply-language rule, already applied), S10 (baseline commit for C4/O2), S11 (INSIGHTS entry folded in), S12 (C1 section citation), T10, T11, O6, O7; revised C1, C4, C5, O2, S7, T8, T9, Decisions, Review hand-off, Risks; gave Constraints/Out-of-scope bullets C*/O* IDs matching the verifier's numbering · Plan Verification of rev 3 (6 partial, 12 cannot verify, security hand-offs, unplanned changes) plus the user's request that every agent reply in the user's language
- rev 5 · 2026-09-25 · Base → `cb195fb` (S10 done); added S13 (README: `tsc --noEmit` gap + rev 5 behaviour), S14 (architecture-reviewer uses `depcruise -T err-long`), S15 (hook: `--dir` limited to the 4 packages and e2e to typecheck/lint, vitest/depcruise/eslint config flags, command substitution incl. unquoted heredocs, unknown mode/profile fails closed, exact hook `self-test`/`bash -n` in `checks`), S16 (symlink self-test moved to a `mktemp` fake project root), S17 (2026-09-25 INSIGHTS entry folded in, user may overturn), S18 (commit of rev 5 files), T12, T13, O8; revised C3, C4, C5, C6, Decisions (checks profile, rejected-in-any-segment, failure policy, reviewers' checks, two new decision paragraphs), S3/S4 details, S7, S8, S10, S11, T8 (no `/agents` wizard; sub-checks b–e done, a partly, f awaits the user), T9, T10, T11, Review hand-off, Risks, O6, Sources; dropped the "C4/O2 before S10" risk · main-session evidence of 2026-09-25 (hook self-tests green, T8 probes, rev 4 architecture review and Plan Verification) and seven change requests (README gap, hook vs agent `--output-type` conflict, symlink location, T8 wording, unplanned INSIGHTS entry, verifier self-test access, four security hand-offs)
- rev 6 · 2026-09-25 · Base stays `cb195fb` (S18 not done). Added:
  - S19: cwd rule — `cd` rejected in both profiles, the hook input `cwd` compared with the project root for `CHECKS_EXACT`/`pnpm`, `--dir=`/`-C<path>`/`--prefix` rejected, `npm` dropped, `git -C`/`--git-dir`/`--work-tree` rejected.
  - S20: argument allowlists for script forms and `exec vitest`/`tsc`/`depcruise`/`eslint`.
  - S21: T12 `rund` asserts the "misconfigured scope-guard" stderr; `bash -n implementer-guard.sh` row.
  - S22: `plan-verifier.md`, `architecture-reviewer.md` and `test-writer.md` Bash constraints synced with the hook; test-writer's "`npm test` from `e2e/`" fixed.
  - S23: README — bare `--dir` names, run from the repo root, cwd rule, allowlists.
  - S24: implementer's `CHECKS_EXACT` INSIGHTS entry folded in, an optional cwd-bypass entry, S11/S17 evidence re-pointed.
  - T14, T15, T16; two new Decisions paragraphs (*cwd rule*, *Argument allowlists*) and a rev 6 security hand-off paragraph.

  Revised: header, C3, C4 (the three non-restricted agent files are editable), C5 (CHECKS_EXACT entry now in the top three), C6, Decisions (readonly list, git flags, checks profile, rejected-in-any-segment, failure policy, reviewers' checks), execution order, S4, S7, S11, S15, S17, S18 (7 files), S2/S3/S4 details, T8 (rev 5 evidence for d/e; new g), T9, T12, Review hand-off, Risks (two new), O8, Sources.

  Why: the main session's rev 5 evidence (hooks and self-tests green; architecture review Clean at 0/17; Plan Verification 34/7/0/3), the security hand-off on the cwd bypass of `CHECKS_EXACT`/`--dir`, and seven change requests. The planner's own re-check found the `--dir=` second-value gap, npm `-C` = `--prefix`, the script-form flag passthrough, vitest reporter / depcruise webpack-config / eslint formatter paths, `git -C` into untrusted clones, and test-writer's `npm test` from `e2e/`.

- rev 7 · 2026-09-25 · Base stays `cb195fb`. Added S25 (point fix: stdin-fed shell invokers, `sed -I`/clustered `-i`/`-f`, attached/clustered `sort -o`, `uniq` output operand) and T17 (18 rows), written and verified by the main session on the user's decision to fix in place instead of a full planner → implementer → review cycle. Known and **not** fixed (recorded, not blocking): T13's `rg 'output-type'` expectation contradicts S22's required "never `--output-type`" line; C5/README say an unresolvable `$CLAUDE_PROJECT_DIR` fails open, the code fails closed; the implementer's ESLint claim is wrong (lint looks up config per file directory, `server/eslint.config.js:16` ignoring `clones/**` is what holds); `pathArgOk` is lexical, no `realpath`. · rev 6 Plan Verification (55/8/0/1) + user decision

**Plan status:** Ready
